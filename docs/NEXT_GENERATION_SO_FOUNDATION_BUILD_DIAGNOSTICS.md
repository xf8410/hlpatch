# Next-generation SO foundation compiler diagnostics

```text
    Updating crates.io index
error: cannot update the lock file /home/runner/work/hlpatch/hlpatch/hachimi_ura_plugin/Cargo.lock because --locked was passed to prevent this
help: to generate the lock file without accessing the network, remove the --locked flag and use --offline instead.
```
