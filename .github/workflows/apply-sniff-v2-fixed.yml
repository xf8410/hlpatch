name: Apply sniff v2 fixed (legacy manual only)

# Legacy one-shot migration workflow. It patches the dedicated
# workbench/sniff-v2 branch and must not run for unrelated feature pushes.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy status
        run: |
          echo "This legacy patch workflow is intentionally manual-only."
          echo "Current plugin features must be edited and built on their own workbench branch."
